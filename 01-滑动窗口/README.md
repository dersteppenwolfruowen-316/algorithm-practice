# 01 · 滑动窗口

## 核心思路

滑动窗口本质上是「用两个指针维护一段区间」，分两大类：

| 类型 | 特征 | 模板 |
|------|------|------|
| **定长** | 窗口大小 k 固定，平移即可 | 进一个出一个 |
| **不定长** | 窗口按条件伸缩 | 右扩 → 不满足则左缩 |

---

## 定长滑动窗口

> 化繁就简，重点是维护窗口内状态的逻辑，而不是指针本身。

**关键细节：**
- 循环数组（首尾相接）→ 下标取模 `i % n`，循环 `2n` 次
- 遇到字符串：转化为子串问题，从"频率/计数"切入，字符频率类可做到 O(n)
- `range` 是左闭右开；循环次数 = `n` 还是 `n+1` 要想清楚
- 弄清楚初始窗口的左右开闭，以及要维护的元素下标

**题目列表：**
| # | 题目 | 要点 |
|---|------|------|
| 30 | 串联所有单词的子串 | 固定字长 × 字数 |
| 438 | 找到字符串中所有字母异位词 | 字符频率 |
| 567 | 字符串的排列 | 字符频率 |
| 1052 | 爱生气的书店老板 | 差值窗口 |
| 1100 | 长度为k的无重复字符子串 | 计数 |
| 1176 | 健身计划评估 | 均值比较 |
| 1343 | 大小为K且平均值大于等于阈值的子数组数目 | 前缀和/直接求和 |
| 1456 | 定长子串中元音的最大数目 | 计数 |
| 2090 | 半径为k的子数组平均值 | 边界处理 |
| 2379 | 得到K个黑块的最少涂色次数 | 转化为固定窗口 |
| 2461 | 长度为K子数组中的最大和 | 无重复元素 |
| 2841 | 几乎唯一子数组的最大和 | 哈希计数 |

---

## 不定长滑动窗口

> 模世界的运算（来自灵神）

**通用模板：**
```python
left = 0
for right in range(n):
    # 1. 入：将 nums[right] 加入窗口
    window.add(nums[right])
    # 2. 缩：不满足条件时收缩左边界
    while not valid(window):
        window.remove(nums[left])
        left += 1
    # 3. 更新答案
    ans = max(ans, right - left + 1)
```

**题目列表：**
| # | 题目 | 要点 |
|---|------|------|
| 1297 | 子串的最大出现次数 | 频率上限 |
| 1423 | 可获得的最大点数 | 逆向思维（最小中间段） |
| 1652 | 拆炸弹 | 循环数组 |
| 1852 | 每个子数组的数字种类数 | 滑动计数 |
| 1888 | 使二进制字符串字符交替的最少反转次数 | 循环数组 |
| 2067 | 等计数子串的数量 | 字符频率 |
| 2134 | 最少交换次数来组合所有的1 II | 循环数组 |
| 2156 | 查找给定哈希值的子串 | 反向滑动 |
| 2200 | 找出数组中的所有k近邻下标 | 距离约束 |
| 2524 | 子数组的最大频率分数 | 频率计算 |
| 3439 | 重新安排会议得到最多空余时间 I | 区间合并 |
| 3652 | 按策略买卖股票的最佳时机 | 双窗口 |
| 3672 | 子数组中加权众数的总和 | 频率统计 |
| 3679 | 使库存平衡的最少丢弃次数 | 单调性 |
| 3694 | 删除子字符串后不同的终点 | 字符串处理 |
| 最少交换 | 最少交换次数来组合所有的1 | 循环数组基础版 |

---

## 单调队列 + 滑动窗口

> 当窗口内需要快速获取 **最大值/最小值** 时，用单调队列将每次操作降到 O(1)。

见 [单调队列+滑动窗口/](单调队列+滑动窗口/) 目录。

---

## 双指针 vs 滑动窗口

| | 双指针 | 滑动窗口 |
|---|--------|---------|
| **区别** | 两指针可相向移动 | 两指针同向，维护区间 |
| **何时用双指针** | 有序数组/链表，寻找满足条件的一对元素 | 子数组/子字符串的最优解 |
| **语言扩展** | `sorted` + two-pointer | `collections.deque` 做单调队列 |
